import "../globals.css";

export const metadata = {
  title: "Contact Us | LearnHealth",
  description: "Book services, reach aout to learn health now",
};

export default function RootLayout({ children }) {
  return (
    <>
        {children}
   </>
  );
}
